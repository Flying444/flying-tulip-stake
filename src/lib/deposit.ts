/* import { useWaitForTransactionReceipt, useWriteContract } from "wagmi"
import { parseEther } from "viem"
import { TDeposit } from '@/types'

export const deposit = async ({ asset, contractAddress, amount }: TDeposit) => {

    const {
        data: hash,
        isPending,
        writeContract
    } = useWriteContract()

    try {

    } catch (error) {

    }
} */